# Backend
SECRET_KEY=your-super-secret-key-change-me
DATABASE_URL=sqlite:///ai_trader.db
# For local docker-compose, frontend is accessed via localhost:3000
FRONTEND_ORIGIN=http://localhost:3000

# Frontend
# In docker-compose, the browser accesses the backend. 
# If using server-side rendering in Next.js to call backend, use http://backend:5000/api
# If client-side, use the public URL (e.g. http://localhost:5000/api)
NEXT_PUBLIC_API_URL=http://localhost:5000/api
